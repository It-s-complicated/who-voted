---
import RouteIndex from "../components/RouteIndex.astro";
import { states } from "../data/states";
---

<RouteIndex
  title="Wer hat gewählt?"
  description="Wahlbeteiligung im Verhältnis zur Bevölkerung. Wähle ein Bundesland aus."
  links={Object.entries(states).map(([state, { name, latestElection }]) => ({
    href: `/${state}/`,
    label: name,
    note: `Letzte Wahl: ${new Intl.DateTimeFormat("de-DE").format(new Date(latestElection))}`,
  }))}
/>

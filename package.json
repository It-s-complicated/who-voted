{
  "name": "who-voted",
  "type": "module",
  "private": true,
  "packageManager": "pnpm@11.25.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "data:download": "node scripts/download-data.mjs",
    "data:build": "node scripts/process-data.mjs",
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "preview": "astro preview",
    "test": "pnpm data:build && pnpm check && astro build"
  },
  "dependencies": {
    "astro": "^7.2.10",
    "d3-sankey": "^0.12.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@types/d3-sankey": "^0.12.5",
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3"
  }
}
